import 'package:flutter/material.dart';

class AppListLayout extends StatelessWidget {
  const AppListLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}